{
	"name": "lyra",
	"version": "0.1.0",
	"description": "A skill library for Claude Code",
	"type": "module",
	"scripts": {
		"test": "bun test",
		"build": "bun scripts/build.ts",
		"link": "./install",
		"deploy": "./scripts/bash/deploy.sh",
		"format": "oxfmt --write",
		"format:check": "oxfmt --check",
		"typecheck": "bun x tsc",
		"lint:skills": "bun scripts/lint-skill.ts",
		"lint:ts": "oxlint scripts/ test/",
		"lint:sh": "bun scripts/lint-sh.ts",
		"format:sh": "bun scripts/lint-sh.ts --write",
		"setup-hooks": "cp scripts/bash/pre-commit.sh .git/hooks/pre-commit && chmod +x .git/hooks/pre-commit && echo 'pre-commit hook installed'"
	},
	"engines": {
		"bun": ">=1"
	},
	"devDependencies": {
		"@types/bun": "^1.3.11",
		"@wasm-fmt/shfmt": "^0.2.6",
		"oxfmt": "^0.42.0",
		"oxlint": "^1.57.0",
		"shellcheck": "^4.1.0",
		"yaml": "^2.8.3"
	}
}
